language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install .
script: python -m pytest

matrix:
  allowed_failures:
    - python: "3.3"
    - python: "3.7-dev"
