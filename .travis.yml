language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
matrix:
  include:
    - python: "3.7"
      dist: xenial
install:
  - pip install masonite-cli
  - craft install
  - pip install -r requirements-dev.txt
script:
  - python -m pytest
  - pre-commit run --all-files --hook-stage=push
